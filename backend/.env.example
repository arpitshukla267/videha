MONGODB_URI=mongodb://localhost:27017/videha-overseas
PORT=4000
FRONTEND_URL=http://localhost:3005

# Cloudinary — copy cloud name + API key + API secret from the same row in the dashboard.
# The API key must allow uploads (not read-only), or seed/CMS uploads will return 403.
CLOUDINARY_CLOUD_NAME=dy331h8c5
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret
CLOUDINARY_FOLDER=videha
