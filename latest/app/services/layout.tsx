import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Services — Videha Overseas",
  description:
    "Export services from Videha Overseas — sourcing, grading, processing, private label, packaging and logistics for international makhana buyers.",
}

export default function ServicesLayout({ children }: { children: React.ReactNode }) {
  return children
}
