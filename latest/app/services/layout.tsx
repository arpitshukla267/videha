import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Services — Videha Overseas",
  description:
    "Export services from Videha Overseas — bulk makhana supply, private label makhana, sourcing, grading, packaging, and logistics for international buyers.",
}

export default function ServicesLayout({ children }: { children: React.ReactNode }) {
  return children
}
